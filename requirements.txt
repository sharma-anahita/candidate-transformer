# ─────────────────────────────────────────────
# Candidate Data Transformation Engine
# Python 3.11+ required
# ─────────────────────────────────────────────

# Core framework
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.3.0,<3.0.0
openai>=1.0.0

# CLI
typer[all]>=0.12.0,<1.0.0

# PDF parsing (resume adapter)
pdfplumber>=0.11.0,<1.0.0

# HTTP client (GitHub adapter, LinkedIn adapter)
requests>=2.32.0,<3.0.0

# Phone normalization
phonenumbers>=8.13.0,<9.0.0

# Date parsing
python-dateutil>=2.9.0,<3.0.0

# Fuzzy string matching (skill deduplication, name matching)
rapidfuzz>=3.9.0,<4.0.0

# JSON Schema validation (output validation)
jsonschema>=4.22.0,<5.0.0

# UI
streamlit>=1.35.0,<2.0.0

# ─────────────────────────────────────────────
# Dev / Test
# ─────────────────────────────────────────────
pytest>=8.2.0,<9.0.0
pytest-cov>=5.0.0,<6.0.0
